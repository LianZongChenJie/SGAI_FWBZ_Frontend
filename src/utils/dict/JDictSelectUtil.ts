// Stub file for JDictSelectUtil - original file not in project
// @jeecg/online package imports this but it was never included in the project

export class JDictSelectUtil {
  static getDictItems(dictCode: string) {
    return [];
  }

  static async loadDict(dictCode: string) {
    return [];
  }

  static getDictLabel(dictCode: string, value: any) {
    return value;
  }
}

export default JDictSelectUtil;